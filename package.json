{
  "name": "jenkins-docker-app",
  "version": "1.0.0",
  "description": "A Dockerized Node.js application for Jenkins CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "react-scripts start",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
